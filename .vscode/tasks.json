{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build-debug",
            "type": "shell",
            "command": "go build -gcflags='all=-N -l' -o ${workspaceFolder}/__debug_bin ${workspaceFolder}",
            "group": "build",
            "problemMatcher": ["$go"]
        }
    ]
}
